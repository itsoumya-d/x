package com.eraser.recovery.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Color Palette for Eraser App
 *
 * Migrated from Flutter app colors (lib/core/constants/app_colors.dart)
 * Matches the original design for consistency
 * Primary color: #11D452 (Green)
 */

// Primary Colors (Green - matches Flutter app exactly)
val Primary = Color(0xFF11D452) // Green - Flutter app primary
val PrimaryDark = Color(0xFF0DB844) // Darker green - Flutter app primaryDark
val PrimaryLight = Color(0xFF4AE07B) // Lighter green - Flutter app primaryLight

// Secondary Colors (Same as primary for consistency)
val Secondary = Color(0xFF11D452) // Green
val SecondaryDark = Color(0xFF0DB844) // Darker green
val SecondaryLight = Color(0xFF4AE07B) // Lighter green

// Accent Colors
val Accent = Color(0xFFF59E0B) // Amber-500
val AccentDark = Color(0xFFD97706) // Amber-600
val AccentLight = Color(0xFFFBBF24) // Amber-400

// Error/Danger Colors
val Error = Color(0xFFEF4444) // Red-500
val ErrorDark = Color(0xFFDC2626) // Red-600
val ErrorLight = Color(0xFFF87171) // Red-400

// Success Colors
val Success = Color(0xFF10B981) // Emerald-500
val SuccessDark = Color(0xFF059669) // Emerald-600
val SuccessLight = Color(0xFF34D399) // Emerald-400

// Warning Colors
val Warning = Color(0xFFF59E0B) // Amber-500
val WarningDark = Color(0xFFD97706) // Amber-600
val WarningLight = Color(0xFFFBBF24) // Amber-400

// Info Colors
val Info = Color(0xFF3B82F6) // Blue-500
val InfoDark = Color(0xFF2563EB) // Blue-600
val InfoLight = Color(0xFF60A5FA) // Blue-400

// Text Colors (Light Theme - matches Flutter app)
val TextPrimary = Color(0xFF102216) // Flutter app textDark
val TextSecondary = Color(0xFF6B7280) // Flutter app textGrey
val TextTertiary = Color(0xFF9CA3AF) // Flutter app textGreyLight

// Text Colors (Dark Theme - matches Flutter app)
val TextPrimaryDark = Color(0xFFFFFFFF) // Flutter app textLight
val TextSecondaryDark = Color(0xFF9CA3AF) // Flutter app textGreyLight
val TextTertiaryDark = Color(0xFF6B7280) // Flutter app textGrey

// Background Colors (Light Theme - matches Flutter app)
val BackgroundLight = Color(0xFFF6F8F6) // Flutter app backgroundLight
val SurfaceLight = Color(0xFFFFFFFF) // Flutter app surfaceLight
val CardLight = Color(0xFFFFFFFF) // White

// Background Colors (Dark Theme - matches Flutter app)
val BackgroundDark = Color(0xFF102216) // Flutter app backgroundDark
val SurfaceDark = Color(0xFF1A2F23) // Flutter app surfaceDark
val CardDark = Color(0xFF1A2F23) // Same as surface

// Border Colors
val BorderLight = Color(0xFFE5E7EB) // Gray-200
val BorderDark = Color(0xFF4B5563) // Gray-600

// Divider Colors
val DividerLight = Color(0xFFE5E7EB) // Gray-200
val DividerDark = Color(0xFF374151) // Gray-700

// Overlay Colors
val OverlayLight = Color(0x80000000) // Black with 50% opacity
val OverlayDark = Color(0x80000000) // Black with 50% opacity

// Gradient Colors
val GradientStart = Color(0xFF6366F1) // Indigo-500
val GradientEnd = Color(0xFF8B5CF6) // Violet-500

// Special Colors
val Streak = Color(0xFFF59E0B) // Amber-500 (for streak indicator)
val Achievement = Color(0xFFFBBF24) // Amber-400 (for achievements)
val Blocked = Color(0xFFEF4444) // Red-500 (for blocked content)
val Protected = Color(0xFF10B981) // Emerald-500 (for protection status)

